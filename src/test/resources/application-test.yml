spring:
  datasource:
    url: jdbc:mysql://${TiDB_USERNAME}:${TiDB_PASSWORD}@gateway01.ap-southeast-1.prod.alicloud.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY
    username: ${TiDB_USERNAME}
    password: ${TiDB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: mature-ox-119917.upstash.io
    port: 6379
    username: ${REDIS_USERNAME}
    password: ${REDIS_PASSWORD}
    ssl: true
    timeout: 10s
    database: 0

logging:
  level:
    com.demo.molly: debug
